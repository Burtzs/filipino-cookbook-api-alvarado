<?php
/**
 * Example configuration file.
 *
 * Copy this file to "config.php" in the project root and fill in your
 * actual local values. "config.php" is listed in .gitignore and will
 * NOT be committed to version control.
 *
 *   cp config.example.php config.php    (Linux/Mac)
 *   copy config.example.php config.php  (Windows)
 */

// Database connection settings
$dbHost = "localhost";
$dbName = "filipino_cookbook_api";
$dbUser = "YOUR_DATABASE_USERNAME";
$dbPass = "YOUR_DATABASE_PASSWORD";

// API authentication token
// Generate your own token; do not reuse example/demo tokens in production.
$apiToken = "YOUR_API_TOKEN";
